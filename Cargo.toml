[package]
name = "ssbu-combat-latency-slider"
version = "1.0.0"
authors = ["alebro"]
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
skyline = "0.2.0"
skyline-web = "0.1.0"
ninput = { git = "https://github.com/blu-dev/ninput", branch = "master" }
once_cell = "1.14.0"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
